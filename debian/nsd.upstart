# nsd - Name Server Daemon

description "Name Server Daemon"
author "Ondřej Surý <ondrej@debian.org>"

start on runlevel [2345]
stop on runlevel [016]

respawn
pre-start script
    mkdir -p /run/nsd
    chown nsd:nsd /run/nsd
end script

exec /usr/sbin/nsd -d
